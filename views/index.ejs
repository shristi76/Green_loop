<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create Account - GREEN LOOP</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet">
  <style>
    body {
      background: url('https://images.unsplash.com/photo-1503264116251-35a269479413?auto=format&fit=crop&w=1470&q=80') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .glass {
      backdrop-filter: blur(20px);
      background-color: rgba(24, 24, 27, 0.75);
      border: 1px solid rgba(255, 255, 255, 0.08);
    }

    input::placeholder {
      color: #9ca3af;
    }
  </style>
</head>
<body class="text-white flex items-center justify-center min-h-screen px-4">
  <div class="w-full max-w-lg p-10 rounded-3xl shadow-xl glass" data-aos="zoom-in">
    <h1 class="text-4xl font-extrabold mb-1 text-center text-green-300 tracking-tight">🌿 GREEN LOOP</h1>
    <p class="text-center text-gray-300 mb-6 text-sm">Join the loop. Plant a better tomorrow.</p>

    <h2 class="text-2xl font-bold mb-6 text-center text-gray-200">Create Your Account</h2>

    <form method="POST" action="/create" class="space-y-5">
      <div>
        <label class="block mb-1 text-sm font-medium text-green-100">Username</label>
        <input type="text" name="username" placeholder="Enter username"
          class="w-full px-4 py-2 bg-zinc-800/60 border border-zinc-600 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-green-400 transition" />
      </div>
      <div>
        <label class="block mb-1 text-sm font-medium text-green-100">Email</label>
        <input type="email" name="email" placeholder="Enter email"
          class="w-full px-4 py-2 bg-zinc-800/60 border border-zinc-600 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-green-400 transition" />
      </div>
      <div>
        <label class="block mb-1 text-sm font-medium text-green-100">Password</label>
        <input type="password" name="password" placeholder="Enter password"
          class="w-full px-4 py-2 bg-zinc-800/60 border border-zinc-600 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-green-400 transition" />
      </div>
      <div>
        <label class="block mb-1 text-sm font-medium text-green-100">Age</label>
        <input type="number" name="age" placeholder="Enter age"
          class="w-full px-4 py-2 bg-zinc-800/60 border border-zinc-600 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-green-400 transition" />
      </div>

      <button type="submit"
        class="w-full mt-3 py-2 bg-green-500 hover:bg-green-600 rounded-lg text-white font-semibold shadow-lg transform hover:scale-105 transition duration-300">
        Create Account
      </button>
    </form>

    <div class="mt-6 text-center">
      <p class="text-sm text-gray-300">Already have an account?</p>
      <a href="/login" class="inline-block mt-2 px-5 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-md font-medium transition duration-300">
        Login
      </a>
    </div>
  </div>

  <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
  <script>
    AOS.init({ duration: 1000, once: true });
  </script>
</body>
</html>
